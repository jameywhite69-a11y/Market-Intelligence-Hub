```powershell
git status
git add app docs
git commit -m "Version 72.0 - Workspace Persistence and Layout Manager"
git tag -a v72.0 -m "Workspace Persistence and Layout Manager"
git push origin chore/release-process
git push origin v72.0
git status
```
