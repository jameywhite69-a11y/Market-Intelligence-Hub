```powershell
git status
git add app docs
git commit -m "Version 93.0 - Portfolio Analytics and Attribution"
git tag -a v93.0 -m "Portfolio Analytics and Attribution"
git push origin chore/release-process
git push origin v93.0
git status
```
