```powershell
git status
git add app docs
git commit -m "Version 66.0 - Live Order Management Framework"
git tag -a v66.0 -m "Live Order Management Framework"
git push origin chore/release-process
git push origin v66.0
git status
```
