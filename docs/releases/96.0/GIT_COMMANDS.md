```powershell
git status
git add app docs
git commit -m "Version 96.0 - Workspace Consolidation and Polish"
git tag -a v96.0 -m "Workspace Consolidation and Polish"
git push origin chore/release-process
git push origin v96.0
git status
```
