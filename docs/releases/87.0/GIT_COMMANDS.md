```powershell
git status
git add app docs
git commit -m "Version 87.0 - Institutional Chart Workspace"
git tag -a v87.0 -m "Institutional Chart Workspace"
git push origin chore/release-process
git push origin v87.0
git status
```
