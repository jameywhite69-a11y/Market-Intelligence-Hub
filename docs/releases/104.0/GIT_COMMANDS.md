```powershell
git status
git add app docs
git commit -m "Version 104.0 - Multi-Account Portfolio Manager"
git tag -a v104.0 -m "Multi-Account Portfolio Manager"
git push origin chore/release-process
git push origin v104.0
git status
```
