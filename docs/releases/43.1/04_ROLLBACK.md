# Rollback

Remove the journal CSS link, script tags, panel section, dock movement line, and delete:

- `app/static/js/journal/`
- `app/static/css/trade_journal.css`

Or run:

```powershell
git revert HEAD
```
