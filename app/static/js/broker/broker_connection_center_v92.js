(function(){const VERSION="92.0";
function render(payload){const panel=document.getElementById("brokerConnectionCenterPanelV92");if(!panel)return;const state=payload?.state||window.MultiBrokerAbstractionV92?.snapshot?.()||{adapters:[]};const rows=state.adapters||[];panel.innerHTML=`<section class="v92-card"><div class="v92-header"><div><h2>Broker Connection Center</h2><span>adapter status and future credential readiness</span></div><strong>${rows.filter(a=>a.connected).length}/${rows.length}</strong></div><div class="v92-list">${rows.map(a=>`<div class="${a.connected?"connected":"planned"}"><b>${a.name}</b><span>${a.type} · ${a.capabilities.join(", ")}</span><em>${a.connected?"connected":"planned"}</em></div>`).join("")}</div></section>`;}
function wire(){window.EventBus?.subscribe?.("broker-abstraction-v92.updated",render);setTimeout(()=>render(),2000);}
window.BrokerConnectionCenterV92={render,version:VERSION};document.addEventListener("DOMContentLoaded",()=>setTimeout(wire,1200));})();
